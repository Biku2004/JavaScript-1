const balance = new Number(100.456789);
// console.log(balance);

//console.log((balance.toFixed())); // gives decimal number till we ask for

//console.log(balance.toPrecision(4)); // round of the elements but priority is the starting elements
// 

const num = 1000000
// console.log(num.toLocaleString("en-IN"));

// console.log(Math.abs(-4)); //abosolute converts -ve to +ve
// console.log(Math.round(4.6)); 
// console.log(Math.floor(4.6)); // takes the lowest
// console.log(Math.ceil(4.6)); // for the top


const min = 10;
const max = 20;

console.log((Math.random() * (max - min + 1)) + min);
